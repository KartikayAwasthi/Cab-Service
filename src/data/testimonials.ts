import type { Testimonial } from "@/lib/types";

// Intentionally empty. Only real, business-supplied reviews should be
// added here — do not fabricate testimonials. Once the client shares
// verified reviews (e.g. from Google), add them as:
// { id, name, rating, review, route }
export const TESTIMONIALS: Testimonial[] = [];
